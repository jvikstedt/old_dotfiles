git clone git@github.com:Janno90/dotfiles.git ~/.vim

Install everything by running ./install

# To add new submodules use this:
git submodule add git://github.com/blabla.git bundle/name_of_the_plugin
