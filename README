git clone git@github.com:Janno90/dotfiles.git ~/.vim

# go to ~ folder and run this line to create link to vimrc file
ln -s ~/dotfiles/vimrc ~/.vimrc

# go to .vim folder and run these commands:
git submodule init
git submodule update

# To add new submodules use this:
git submodule add git://github.com/blabla.git bundle/name_of_the_plugin

# If you want to disable capslock and use it to go normal mode on vim use this
# Add to .bash_profile
# Load .Xmodmap
if [ -f ~/.vim/Xmodmap ]; then
  xmodmap ~/.vim/Xmodmap
fi


# vim-gtk has clipboard support, atleast my xfce defaulted to vim-tiny
# and had no support for clipboards
